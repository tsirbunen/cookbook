const soundSettingsTitle = 'Sound effects'

const mainInfo =
  'User actions (like clicking a button) sometimes have a sound effect attached to them. To enable / disable these sound effects, use the switch below.'

const valuePrefix = 'Sound effects are '
const enabledText = 'ON'
const disabledText = 'OFF'

const examplesInfo =
  'There are different kinds of sounds for different kinds of events. Click the buttons below to hear the sounds used in the app.'

const turnSoundsOnReminder = 'Note that the sound effects must be turned ON for the buttons to work.'

const positiveButtonLabel = 'POSITIVE'
const negativeButtonLabel = 'NEGATIVE'

export const labels = {
  soundSettingsTitle,
  mainInfo,
  valuePrefix,
  enabledText,
  disabledText,
  examplesInfo,
  turnSoundsOnReminder,
  positiveButtonLabel,
  negativeButtonLabel
}
