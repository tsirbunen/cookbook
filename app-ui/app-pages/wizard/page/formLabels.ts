const title = 'Title'
const titlePlaceholder = 'Add a title...'
const titleInfo = 'Give your recipe a concise and descriptive title.'

const description = 'Description'
const descriptionPlaceholder = 'Add a description...'
const descriptionInfo =
  'Describe the recipe in a few words, for example suggest occasions when it can be served, or what makes it special.'

const tags = 'Tags'
const tagsPlaceholder = 'Add a tag...'
const tagsInfo = 'Add simple, short tags to help you (or other users) find your recipe more easily.'

const language = 'Language'
const languagePlaceholder = 'Add a language...'
const languageInfo = 'In which language is the recipe written?'

const ovenNeeded = 'Oven needed'
const ovenNeededInfo = 'Does the recipe require an oven?'
const isPrivate = 'Is private'
const isPrivateInfo = 'Are you the only one to see the recipe?'

const booleanYes = 'Yes'
const booleanNo = 'No'
const booleanPlaceholder = 'No'

const groupTitlePlaceholder = 'Add a group title (optional)...'
const ingredientGroups = 'Ingredients'
const ingredientGroupsInfo = 'Ingredients can be listed as such or divided into separate groups.'
const amountPlaceholder = 'Amount?'
const unitPlaceholder = 'Unit?'
const namePlaceholder = 'Name'
const instructionGroups = 'Instructions'
const instructionGroupsInfo = 'Instructions can be listed as such or divided into separate groups.'
const contentPlaceholder = 'Content'

const signInRequired = 'You need to sign in to create a recipe.'
const photosInfo =
  'Add photos to make the recipe more appealing. Drag and drop the relevant files here. You can add a maximum of 4 photos. To remove a photo, click on it.'
const photosTitle = 'Photos'
const modifyRecipe = 'Modify recipe'

export const formLabels = {
  signInRequired,
  modifyRecipe,
  photosInfo,
  photosTitle,
  title,
  titlePlaceholder,
  titleInfo,
  description,
  descriptionPlaceholder,
  descriptionInfo,
  tags,
  tagsPlaceholder,
  tagsInfo,
  language,
  languageInfo,
  languagePlaceholder,
  ovenNeeded,
  ovenNeededInfo,
  isPrivate,
  isPrivateInfo,
  booleanYes,
  booleanNo,
  booleanPlaceholder,
  ingredientGroups,
  ingredientGroupsInfo,
  groupTitlePlaceholder,
  amountPlaceholder,
  unitPlaceholder,
  namePlaceholder,
  instructionGroups,
  instructionGroupsInfo,
  contentPlaceholder
}
