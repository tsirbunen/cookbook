fragment AccountFull on Account {
  id
  username
  phoneNumber
  isVerified
  token
}

mutation DeleteAccount($id: Int!) {
  deleteAccount(id: $id) {
    __typename

    ... on GeneralSuccess {
      successMessage
    }
    ... on GeneralError {
      errorMessage
    }
  }
}
