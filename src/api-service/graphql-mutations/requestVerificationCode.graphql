mutation RequestVerificationCode($phoneNumber: String!) {
  requestVerificationCode(phoneNumber: $phoneNumber) {
    __typename

    ... on GeneralSuccess {
      successMessage
    }
    ... on GeneralError {
      errorMessage
    }
  }
}
